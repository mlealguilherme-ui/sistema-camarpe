# ============================================
# Sistema Camarpe — Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env.local e preencha os valores reais
# NUNCA comite o .env.local com credenciais reais
# ============================================

# --------------------------------------------
# Banco de Dados (obrigatório)
# --------------------------------------------
# Conexão pooled (para a aplicação — use a URL com -pooler no Neon)
DATABASE_URL="postgresql://usuario:senha@host-pooler:5432/camarpe_db?sslmode=require"

# Conexão direta (para migrações Prisma — sem -pooler no Neon)
DIRECT_URL="postgresql://usuario:senha@host:5432/camarpe_db?sslmode=require"

# --------------------------------------------
# Autenticação JWT (obrigatório - mínimo 32 chars)
# Gere com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# --------------------------------------------
JWT_SECRET="sua-chave-secreta-com-pelo-menos-32-caracteres-aqui"

# --------------------------------------------
# Criptografia de Credenciais (obrigatório - mínimo 32 chars)
# Gere com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# --------------------------------------------
ENCRYPTION_KEY="sua-chave-de-criptografia-com-pelo-menos-32-caracteres"

# Opcional: salt customizado para derivação de chave
# ENCRYPTION_SALT="salt-customizado"

# --------------------------------------------
# URL da Aplicação (para links de recuperação de senha)
# --------------------------------------------
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --------------------------------------------
# Ambiente
# --------------------------------------------
NODE_ENV="development"
