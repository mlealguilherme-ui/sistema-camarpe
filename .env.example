# Sistema Camarpe - variáveis de ambiente
# Copie para .env e preencha. Não commite .env.

# Obrigatório
# Em produção (Vercel/serverless): use connection pooling. Ex.: Neon use a URL *-pooler (ex.: ep-xxx-pooler.sa-east-1.aws.neon.tech) para menos latência e menos conexões.
DATABASE_URL="postgresql://user:pass@host:5432/dbname?sslmode=require"
JWT_SECRET="sua-chave-secreta-com-pelo-menos-32-caracteres"

# Upload de arquivos (projetos)
# Desenvolvimento: pasta local (ex.: ./uploads)
# Produção (Vercel/serverless): use storage externo (S3, Vercel Blob) e configure a URL/path conforme sua integração
UPLOAD_DIR="./uploads"

# Opcional (NextAuth se usar)
NEXTAUTH_SECRET="opcional"
NEXTAUTH_URL="http://localhost:3000"
